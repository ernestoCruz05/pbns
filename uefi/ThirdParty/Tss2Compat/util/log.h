#ifndef PBNS_TSS2_COMPAT_LOG_H
#define PBNS_TSS2_COMPAT_LOG_H

#include "util/aux_util.h"

#define LOG_ERROR(...) do { } while (0)
#define LOG_WARNING(...) do { } while (0)
#define LOG_INFO(...) do { } while (0)
#define LOG_DEBUG(...) do { } while (0)
#define LOG_TRACE(...) do { } while (0)
#define LOGBLOB_ERROR(...) do { } while (0)
#define LOGBLOB_WARNING(...) do { } while (0)
#define LOGBLOB_INFO(...) do { } while (0)
#define LOGBLOB_DEBUG(...) do { } while (0)
#define LOGBLOB_TRACE(...) do { } while (0)

#endif
