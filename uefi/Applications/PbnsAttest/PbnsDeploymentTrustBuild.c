/* Angle brackets force resolution through the build's fresh private include directory. */
/* NOLINTNEXTLINE(bugprone-suspicious-include): the generated C file is the build input. */
#include <PbnsDeploymentTrust.c>
